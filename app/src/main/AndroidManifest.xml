<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicePermission"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"
        tools:ignore="ForegroundServicePermission"/>
    <!-- Required on Android 13+ (API 33+) to post the foreground-service notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".TrainTrackerApp"
        android:theme="@style/Theme.TrainTracker"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ServiceDetailActivity"/>
        <activity android:name=".StationBoardActivity"/>
        <activity android:name=".StationInfoActivity"/>

        <!--
            DarwinService — foreground service that maintains the STOMP WebSocket
            connection to Darwin Push Port for real-time train updates.

            foregroundServiceType="dataSync" is required on Android 14+ (API 34+)
            for any service that holds a persistent network connection. Without it,
            startForegroundService() will throw on API 34+ devices.

            android:exported="false" ensures only this app can start the service.
        -->
        <service
            android:name=".DarwinService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

    </application>
</manifest>